@model Financial.Core.ViewModels.Asset.DeleteViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Asset</h2>

<div>
    <h4>Are you sure you want to delete this record?</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AssetName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AssetName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AssetTypeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AssetTypeName)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "Asset", FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions col-md-offset-1 no-color">
            <input type="submit" value="Delete" class="btn btn-default" />
            @Html.ActionLink("Cancel", "Index", "Asset", null, new { @class = "btn btn-default" })
        </div>
    }
</div>
